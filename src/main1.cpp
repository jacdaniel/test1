

#include <stdio.h>

int main(int argc, char **argv)
{
  fprintf(stderr, "ok\n");
  fprintf(stderr, "on continue sur master"); 
  fprintf(stderr, "encore un");
  fprintf(stderr, "et un de plus sur master")
  fprintf(stderr, "re un autre sur master")
  fprintf(stderr, "ok branch dev1\n");
  fprintf(stderr, "on continue sur dev1");
  return 0;
}
